{{-- 
    label       : field label
    name        : field name
    other props : other input attributes such as type, size, placeholder
--}}

<div class="field">
    @if(label)
        <label for="{{name}}">{{label}}</label> 
    @end
    @if(type=="submit") 
        <input id="{{name}}" class="ui button" {{ $props.except(['label']).toAttrs() }}>
    @else
        <input id="{{name}}" {{ $props.except(['label']).toAttrs() }}>
    @end
</div>